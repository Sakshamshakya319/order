<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>New Order</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  {{#if orders}}
  <table>
    <thead>
      <tr>
        <th>Item Name</th>
        <th>Item Price</th>
        <th>Units Bought</th>
        <th>Payable Amount</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      {{#each orders}}
        <tr>
          <td>{{this.itemName}}</td>
          <td>${{this.itemPrice}}</td>
          <td>{{this.unitsBought}}</td>
          <td>${{this.payableAmount}}</td>
          <td>
            <a href="/edit/{{this._id}}">Edit</a>
            <a href="/delete/{{this._id}}" onclick="return confirm('Are you sure you want to delete this order?')">Delete</a>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
{{else}}
  <p>No orders found. <a href="/create">Create a new order</a>.</p>
  
{{/if}}
</body>
</html>